@page "/"
@inject GamesClient GamesClient

<PageTitle>Game Catalog</PageTitle>

<div class="mt-2">
    <a class="btn btn-primary" role="button" href="/edit-game">New Game</a>
</div>

@if (_games is null)
{
    <p>
        <em>
            Loading....
        </em>
    </p>
}
else
{
    <div class="table-responsive">
        <table class="table table-striped table-bordered table-hover mt-2">
            <thead class="table-dark">
            <th>Name</th>
            <th>Genre</th>
            <th class="text-end">Price</th>
            <th>Release Date</th>
            <th></th>
            </thead>
            <tbody class="table-group-divider">
            @foreach (var game in _games)
            {
                <tr>
                    <td>@game.Name</td>
                    <td>@game.Genre</td>
                    <td class="text-end">@game.Price.ToString("C2")</td>
                    <td>@game.ReleaseDate.ToString()</td>
                    <td>
                        <div class="d-flex justify-content-center gap-3">
                            <a href="/edit-game/@game.Id">
                                <i class="bi bi-pencil-fill text-success"></i>
                            </a>
                            <a role="button" data-bs-toggle="modal" data-bs-target="@GetDeleteModalId(game)">
                                <i class="bi bi-trash-fill text-danger"></i>
                            </a>
                            <DeleteGame Game="@game"></DeleteGame>
                        </div>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
}

@code
{
    private GameSummary[]? _games;

    protected override void OnInitialized()
    {
        _games = GamesClient.GetGames();
        base.OnInitialized();
    }

    private static string GetDeleteModalId(GameSummary game) => $"#{DeleteGame.GetModalId(game)}";
}
